# FastAPI Auth Service

## 🚀 Инструкция по запуску

1. Скопируйте `.env.example` и переименуйте его в `.env`:
   ```bash
   cp .env.example .env
   ```

2. Соберите Docker-образ и поднимите контейнеры:
   ```bash
   docker build --tag fastapi:latest .
   docker compose up
   ```

После запуска приложение будет доступно по адресу:  
`http://localhost:8000`

---

## 🧩 Основные технологии

- **FastAPI** — веб-фреймворк для REST API  
- **PostgreSQL** — основная база данных  
- **SQLAlchemy** — ORM для работы с БД  
- **Alembic** — миграции схемы  
- **bcrypt** — создание и валидация токенов  
- **Docker / Docker Compose** — контейнеризация и оркестрация

---

## 🏗️ Архитектура приложения

Приложение реализует систему аутентификации через JWT-токены, управляемые сессионной моделью:

- **Логин:** при успешной авторизации создаётся `session_uuid`, который привязывается к токенам доступа и обновления. Токены записываются в куки.  
- **Рефреш:** при истечении access-токена используется refresh-токен, чтобы выдать новый.  
- **Логаут:** уничтожает активную сессию и все связанные с ней токены.  
- **Аборт сессий:** позволяет завершить все активные сессии пользователя.  
- **Просмотр сессий:** можно получить список всех активных сессий, включая их IP-адреса и время создания.

---

## ⚙️ Как работает система сессий

1. При логине генерируется уникальный UUID для сессии.  
2. Этот UUID сохраняется в базе и связывается с токенами.  
3. При запросах токены сверяются с активной сессией.  
4. Если пользователь выполняет logout или abort, сессия и все токены, связанные с ней, становятся недействительными.  
5. Сессии можно просматривать через API.

---

## 📂 Контейнеризация

- `Dockerfile` — сборка сервиса FastAPI  
- `docker-compose.yml` — оркестрация (FastAPI + PostgreSQL)
